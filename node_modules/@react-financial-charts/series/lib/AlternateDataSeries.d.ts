import * as React from "react";
export interface AlternateDataSeriesProps<TData> {
    readonly data: TData[];
}
export declare const AlternateDataSeries: <TData>({ data, children, }: React.PropsWithChildren<AlternateDataSeriesProps<TData>>) => JSX.Element;
